App As appinfo:
    BackEnabled: =true
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

    SharePointIntegration As hostControl.SharePointHostControlVariant:
        DataSource: =[@'Quality Problem Status']
        OnCancel: =ResetForm(frmQPS)
        OnEdit: =EditForm(frmQPS)
        OnNew: |-
            =//  新規作成
            NewForm(frmQPS)
        OnSave: |-
            =// エラーフラグとエラーメッセージ
            Set(gvErrorFlag, false);
            Set(gvErrMsg, Blank());
            
            //  入力チェック
            With(
                // 入力データ取得 
                {strSite:ddlSite.Selected.Value,
                 strClaimDescription:rdbClaimDescriptionType.Selected.Value,
                 strCustomer:ddlCustomer.Selected.Customer,
                 strMediaProgram:cmbMediaProgram.Selected.Value,
                 strP1Process:ddlP1RCProcess.Selected.Process,
                 strP1ActualCause:cmbP1RCActualCause.Selected.Value},
                
                If(
                    IsBlank(strSite),
                        Set(gvErrMsg, "Please select a Site！"),
                    IsBlank(strClaimDescription),
                        Set(gvErrMsg, "Please select a Claim description！"),
                    IsBlank(strCustomer),
                        Set(gvErrMsg, "Please select a Customer！"),
                    IsBlank(strMediaProgram),
                        Set(gvErrMsg, "Please select a Media Program！"),
                    IsBlank(strP1Process),
                        Set(gvErrMsg, "Please select a Process1 Process！"),
                    IsBlank(strP1ActualCause),
                        Set(gvErrMsg, "Please select a Process1 Actual cause！")
                );
                If(!IsBlank(gvErrMsg),
                    Notify(gvErrMsg, NotificationType.Warning);
                    Set(gvErrorFlag, true)
                )
            );
            
            If(!gvErrorFlag,
                    //  Status更新
                    
            
                    
                    //  保存処理
                    SubmitForm(frmQPS)
            )
        OnView: =ViewForm(frmQPS)
        SelectedHidden: =LookUp([@'Quality Problem Status'], ID = Self.SelectedListItemID)

